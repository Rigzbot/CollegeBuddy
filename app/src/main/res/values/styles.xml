<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="ThemeOverlay.search" parent="Theme.CollegeBuddy">
        <!-- Text color -->
        <item name="android:editTextColor">@color/white</item>
        <!-- Hint text color -->
        <item name="android:textColorHint">@color/gray</item>
    </style>

</resources>